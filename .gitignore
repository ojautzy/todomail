# macOS
.DS_Store

# Claude Code local settings
.claude/settings.local.json

# Python
__pycache__/
*.pyc
*.pyo

# Runtime data (created by /start in user's working directory)
inbox/
todo/
mails/
to-clean-by-user/
to-send/
to-work/
to-brief/
docs/
memory/
consult.md
.todomail-config.json

# Runtime du plugin pour le workspace (alpha.8+) : state.json,
# memory_cache.json, invalidate.txt, hooks.log, retry_request.txt,
# errors_dismiss.txt — tout est dans ce dossier unique.
.todomail/

# Refactoring plan and per-phase prompts (working documents, not part of the plugin)
REFACTOR_PLAN.md
refactor-prompts/
